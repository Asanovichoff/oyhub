global:
  scrape_interval: 15s

scrape_configs:
  - job_name: oyhub
    static_configs:
      # The dashboard runs on the HOST (next to the SQLite data) — SQLite WAL
      # over Docker bind mounts is unreliable, so don't containerize the reader.
      - targets: ["host.docker.internal:8400"]
